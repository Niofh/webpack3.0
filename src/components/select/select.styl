@import '~css/style/color.styl'

.choices {
  margin-bottom 0
}

// 下拉
.choices__inner, .choices__input {
  background $backgroutfff
}

.choices__list--dropdown .choices__item--selectable.is-highlighted {
  background-color: $backgrout00bcd4;
  color: $backgroutfff
}

.choices__list--dropdown {
  z-index 1000
}